server:
  port: 9000
  include-message: always

spring:
  datasource:
    url: jdbc:mysql://localhost/difficult?createDatabaseIfNotExist=true
    username: root
    password: Rodo2506
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:

    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true